# Transactions date validation schema

O schema de transações passou a validar `date` na entrada. Assim, a API não depende mais de conversão tardia na camada de serviço para um campo central do contrato.
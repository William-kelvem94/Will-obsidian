# Dashboard debt counts aligned

O dashboard agora calcula `totalDebts`, `activeDebts`, `overdueDebts` e `debtsAmount` a partir da mesma base de dívidas abertas. Dívidas `PAID` e `CANCELLED` deixam de inflar os números do painel.
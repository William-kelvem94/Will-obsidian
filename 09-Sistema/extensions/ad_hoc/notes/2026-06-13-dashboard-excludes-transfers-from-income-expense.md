# Dashboard excludes transfers from income and expense

O cálculo mensal do dashboard agora ignora transações de transferência ao compor `monthlyIncome` e `monthlyExpense`. Isso evita inflar os valores brutos do mês com movimentos internos entre contas.
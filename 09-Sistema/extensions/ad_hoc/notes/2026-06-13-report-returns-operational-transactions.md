# Report returns operational transactions

O relatório de fluxo de caixa agora devolve `transactions` filtradas para transações operacionais, em vez de misturar transferências internas com o resumo já depurado.
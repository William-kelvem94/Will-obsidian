# Contrato de categorias

A rota `GET/POST /api/categories` passou a usar helpers comuns para lista e criação, reduzindo duplicação e deixando a serialização mais previsível para quem consome o financeiro.